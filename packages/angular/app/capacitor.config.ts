import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular/app',
  webDir: '../../../dist/packages/angular/app',
  bundledWebRuntime: false,
};

export default config;
